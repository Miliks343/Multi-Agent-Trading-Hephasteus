# meeting notes — 2026-05-06, 7pm

## status

- **A** (obs extractor) — Lollo, merged ✅
- **B** (action translator) — Neil, merged ✅
- **D** (metrics harness) — Neil, merged ✅
- **F** (constant-spread baseline) — Lollo, merged ✅
- **ABIDES hello-world notebook** — Lollo, merged ✅
- **E** (toy training pipeline) — Pavel, branch open, awaiting review
- **C** (env glue) — not started
- **reward design** — not started

basically everyone but me delivered. nice work.

## small repo cleanup

- `log/1778061202/` and `log/1778061225/` (ABIDES summary logs) got committed by accident
- add `log/` to `.gitignore`
- also: README still says "ABIDES install — see Lollo's notes (incoming)". needs filling in.

## target: may 10 demo

- **floor:** baseline running through real ABIDES, metrics computed, plots produced. no RL.
- **ceiling:** first PPO training run kicked off, with live metrics in TensorBoard.
- public commitment level: stretch, not commitment (keep prior framing).

## proposed assignments

### Pavel — C + reward design v1

**C (env glue):** PettingZoo `parallel_env` class wiring A's `extract_observation` + B's `translate_action` with `reset` / `step` / `done`. Drives an ABIDES sim under the hood. Emits per-step trajectory data in D's format via the `info` dict.

**reward v1:** propose a function over per-step trajectory deltas. starting point: PnL increment minus quadratic inventory penalty (Avellaneda-Stoikov flavoured). pure function so it's testable independently of C.

tonight after the meeting.

### Lollo — floor demo + ABIDES install docs

**floor demo:** one script or notebook that:
1. clones `rmsc03_simple.py` → `rmsc03_with_baseline.py`, drops in `ConstantSpreadMarketMaker`
2. captures per-wakeup snapshots `(timestamp, inventory, cash, mid_price, fill)` from the baseline agent into a buffer
3. converts the buffer to a D-format `Trajectory` (ns → float seconds, everything else stays cents)
4. runs `compute_all(traj)`
5. plots PnL curve + inventory histogram

**watch out for:**
- `Trajectory.__post_init__` enforces integer-cents dtype on cash / mid_price / inventory — one accidental float division will throw
- "fill" on a wakeup with no trade is `0` (signed int)
- add `log/` to `.gitignore` while you're in there
- write the ABIDES install steps into README so the rest of us can run your notebook

~2 days. independent of C, runs in parallel.

### Neil — live-metrics SB3 callback

**why:** `ep_rew_mean` only tells us the agent is optimising the reward we wrote. it doesn't tell us whether the agent is actually market-making well. sharpe / drawdown / inventory stats do.

**what:** subclass `BaseCallback`. accumulate `(ts, inv, cash, mid, fill)` per agent across a rollout from `self.locals["infos"]`. on `_on_rollout_end`, build a `Trajectory`, call `compute_all`, log each metric via `self.logger.record("marl/<name>", value)`. clear buffer.

**watch out for:**
- multi-agent: with SuperSuit's `pettingzoo_env_to_vec_env_v1`, each vec slot is one agent. decide: one aggregate trajectory or per-agent metrics. per-agent is more informative.
- buffer must reset each rollout
- sharpe over a 2048-step window will be noisy — that's fine, the trend is what matters

**second task TBD.** I want to land C and see the pipeline run end-to-end before scoping more downstream work. will scope by Friday.

~2 days for the callback alone.

## the integration contract

one line we should all agree on so the three streams converge:

```
env.step() returns info[agent] = {
    "traj_row": (timestamp_s, inventory, cash_cents, mid_cents, fill_signed_int)
}
```

with this agreed, Neil can build the callback against the contract tonight without waiting for C.

## sequencing

- **tonight (Pavel):** C + reward v1. push branch.
- **wed/thu (Lollo):** floor demo running end-to-end.
- **wed/thu (Neil):** callback against the agreed contract.
- **fri:** I scope Neil's second task. merge C + reward.
- **fri/sat:** swap E from `simple_spread_v3` to `MarlLobEnv`. kick off first PPO run.
- **may 10:** demo.

## decisions to make in the meeting

1. agree on the `info["traj_row"]` contract above (or push back on it).
2. reward v1 starting point — Avellaneda-Stoikov-style PnL minus inventory penalty? open to other suggestions, especially Lollo since he did the lit review.
3. merge E now (toy PPO, unmerged since may 2) or wait and merge with C.
4. public commitment level for may 10. keep "stretch, not commitment" framing?
