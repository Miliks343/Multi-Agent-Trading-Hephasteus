# Multi-Agent-Trading-Hephasteus

Multi-agent reinforcement learning in limit order books (ABIDES + PettingZoo + SB3).

## Install

Requires Python 3.11+.

```bash
pip install -e .
```

## Run tests

```bash
pytest
```

## Installing ABIDES

ABIDES is installed separately — see Lollo's notes (incoming).
